package af.crm.webplatform.lib;

import java.time.LocalDate;
import java.util.Calendar;
import java.util.GregorianCalendar;

public class DataConverter {
/*    public static GregorianCalendar stringToCalendar(String incDate) {
        GregorianCalendar date = null;
        String[] splitData = incDate.split("-");
        date = new GregorianCalendar(Integer.getInteger(splitData[0]),
                Integer.getInteger(splitData[1]),
                Integer.getInteger(splitData[2]),
                0, 0, 0);
        return date;
    }

    public static LocalDate stringToLocalDate(String incDate) {
        LocalDate date = null;
        String[] splitData = incDate.split("-");
        date = LocalDate.of(Integer.parseInt(splitData[0]),
                Integer.parseInt(splitData[1]),
                Integer.parseInt(splitData[2]));
        return date;
    }*/
}
